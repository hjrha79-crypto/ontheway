재검증 결과 승인. 2단계 삭제 작업 진행.

[승인 범위]
위 보고서의 "최종 확정 삭제 범위" 그대로 진행.
VoiceManager/VoiceControl 파일 삭제 + 참조 제거 + RECORD_AUDIO 제거.
acceptCurrentCall, ACCEPT_BUTTON_TEXTS, TTS는 유지.
startVoiceRecognition/doAccept/acceptBtn/resultCard UI 레거시는 
별도 태스크(v3.19 UI 정리)로 분리.

[진행 순서]
1. Git 커밋 먼저 (현재 상태 스냅샷)
   - 커밋 메시지: "checkpoint: before VoiceManager removal"
2. 파일 삭제 (VoiceManager.kt, VoiceControl.kt)
3. 참조 제거 (각 파일 수정)
4. AndroidManifest RECORD_AUDIO 제거
5. 빌드 시도 → 컴파일 에러 해결
6. 전체 유닛테스트 실행 → 통과 확인
7. APK 빌드

[빌드 에러 발생 시]
- 에러 로그 전체 보고 후 수정
- 자체 판단으로 추가 코드 생성 금지 (userMemory 원칙)
- 단순 참조 제거로 안 풀리면 보고

[단계 완료 보고 요청]
각 단계 종료 시 다음 보고:
- 실제 변경된 파일 경로 + 줄 수
- 추가/삭제된 코드 요약 (핵심만)
- 다음 단계 진행 가능 여부

[검증 체크리스트 (작업 완료 후)]
- 전체 컴파일 성공
- 기존 유닛테스트 88개 전부 통과
- 새로운 테스트 실패 없음
- APK 빌드 성공
- APK 크기 감소 확인 (VoiceManager/VoiceControl 제거 효과)

3단계 실제 검증(설치 후 유튜브/키보드 테스트)은 
대표님이 설치 후 직접 확인. 지금은 빌드까지만.

시작해줘.